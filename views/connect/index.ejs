<h2 class="connect-title">Let's CONNECT!</h2>

<div class="connect-container">
  <div class="connect-container-left">
    <form action="/connect/submit" method="post">
      <div class="connect-name-email-container">
        <div>
          <input type="text" name="name" placeholder=<%= t("yourName") %> required>
        </div>
        <div>
          <input type="email" name="email" placeholder=<%= t("yourEmail") %> required>
        </div>
      </div>

      <div class="connect-musician-title">
        <span>YOUR PREFERRED MUSICIAN</span>
      </div>
      <div id="musicianList">
        <div class="musicianDropdown">
          <select name="musicians[]">
            <option value="" disabled selected hidden>
              请选择音乐人
            </option>
            <% musicians.forEach(musician => { %>
              <option value="<%= musician.mid %>" 
                <%= musicianName && musician.name === musicianName ? 'selected' : '' %>
              >
                <%= musician.name %>
              </option>
            <% }) %>
          </select>
          <button type="button" onclick="removeDropdown(this)">&#10005;</button>
        </div>
      </div>
      <button class='addMusician-button' type="button" onclick="addDropdown()">
        <%= t("addMusician") %>
      </button>
      <textarea name="message" placeholder="Your message" required></textarea>

      <div class="connect-button">
        <button type="submit">
          <span>CONNECT</span>
          <span class="circle"></span>
        </button>
      </div>      
    </form>
  </div>

  <div class="connect-container-right">
    <div>
      <div class="navi"><a href='/musicians'>MUSICIANS</a></div>
      <div class="navi"><a href='/about-us'>ABOUT US</a></div>
      <div>
        <img src="/img/index_comment_section/5.png">
      </div>
    </div>
  </div>
</div>

<div class="connect-bottom-logo">
  <img src="/img/logo_text_black.png">
</div>

<div id="musiciansData" data-musicians='<%- JSON.stringify(musicians) %>' style="display: none;"></div>


<script src="javascripts/addRemoveButton.js"></script>
